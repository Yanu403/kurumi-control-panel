/** App configuration */

export const APP_NAME = 'Kurumi Control Panel';
export const APP_VERSION = '1.0.0';

/** Default API base URL (overridden by localStorage if set). */
export const DEFAULT_API_BASE = 'http://localhost:9122';
